$kind: http-request
name: 'SLA compliance by priority'
method: GET
url: '{{base_url}}/reports/sla'
description: Returns SLA compliance metrics broken down by ticket priority.
order: 2000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Status 200', () => pm.response.to.have.status(200));
