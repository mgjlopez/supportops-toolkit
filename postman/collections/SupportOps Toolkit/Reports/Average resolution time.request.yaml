$kind: http-request
name: 'Average resolution time'
method: GET
url: '{{base_url}}/reports/resolution'
description: Returns the average time taken to resolve tickets.
order: 3000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Status 200', () => pm.response.to.have.status(200));
